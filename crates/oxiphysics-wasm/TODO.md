# oxiphysics-wasm TODO

Updated: 2026-05-17 | Version: 0.1.1

---

## Phase 1: Foundation ✅

- [x] Define error types (`error` module) and shared primitives (`types`)
- [x] Implement math helpers (`math_helpers` submodules: vectors, quaternions, transforms)
- [x] Add unit tests (826 tests passing)

## Phase 2: Engine Logic ✅

- [x] `engine` module — self-contained simulation engine (step/state/world submodules)
- [x] `physics_config` — global simulation parameters
- [x] `simulation_api` — top-level simulation entry points (`WasmPhysicsEngine`)
- [x] `sim_controls` — play/pause/reset/step controls
- [x] `body_query` — rigid body state queries
- [x] `particle_system` — particle update and management
- [x] `events` — collision/sensor event queue

## Phase 3: Bridge Modules ✅

- [x] `analytics_bridge` — performance metrics export
- [x] `constraint_bridge` — constraint/joint configuration bridge
- [x] `debug_tools` — debug info extraction (`DebugInfo`)
- [x] `fluid_bridge` — SPH/LBM fluid state bridge
- [x] `io_bridge` — scene serialization in/out
- [x] `js_api` — JavaScript-facing API surface
- [x] `material_bridge` — material parameter bridge
- [x] `renderer` — render data extraction for WebGL

## Phase 4: Documentation ✅

- [x] All 1,276 public items documented
- [x] Key type exports: `WasmPhysicsEngine`, `Vec3Wasm`, `QuatWasm`, `TransformWasm`, `RigidBodyConfig`, `SimulationConfig`, `ColliderConfig`, `ContactResult`, `RaycastResult`, `BodyState`, `DebugInfo`

## Phase 5: wasm-bindgen Packaging ✅ (0.1.1)

- [x] Add `wasm-bindgen` dependency to `Cargo.toml`
- [x] Annotate exported types and functions with `#[wasm_bindgen]` (engine/mod.rs — `WasmPhysicsEngine` JS class with full API)
- [x] Configure `wasm-pack` build pipeline (run `wasm-pack build --target web`)
- [x] Generate JS/TS glue code (auto-generated by wasm-pack)
- [ ] Publish npm package (0.2.0)

## Phase 6: Runtime Integration ✅ (0.1.1)

- [x] Web Worker / `SharedArrayBuffer` multi-threaded support (`web_worker` — `SharedStateBuffer`, `SimCommand`, `SimResult`, `WorkerBridge`, `WorkerRuntime`)
- [x] WGPU/WebGL render integration (`webgl_bridge` — `WebGlBridge`: procedural sphere/box/capsule/cylinder/plane meshes, `WebGlRenderFrame` instance arrays, `WebGlCamera` with perspective + look-at)
- [x] Performance benchmarks vs. Rapier WASM (`wasm_bench` — `WasmBenchHarness`, rigid-body/particle/SPH timing, `RapierReference` comparison table)
- [x] Examples: browser-based rigid body demo, fluid demo (0.2.0)

## Phase 7: Umbrella Module Coverage for Web Demos (0.2.0)

> **Goal:** Expose the Phase 13-19 umbrella modules that matter for interactive browser demos. Focus is on gameplay / interaction / observability rather than full backend parity (Python gets the full surface in its Phase 6).

### 7.1 Gameplay & Interaction (high priority)
- [x] (2026-05-06) `character` — `CharacterController::move_and_slide/jump` — headline WASM demo: WASD + mouse-look capsule character on terrain
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `trigger` — `TriggerVolume` enter/exit events — for pickup zones, goal areas, damage regions in demos
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `animation` — `AnimationClip`, `AnimationPlayer` — cinematic sequences driven from JS
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `query` — `Ray`, `raycast` — mouse-pick from browser, hover highlights, line-of-sight
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `rope` — `Rope::step` + `iter_segments` for JS render consumption
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `ik` — `IkChain`, `IkSolver` — reach / look-at demos
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `navmesh` — `NavMesh::from_triangles`, `Query` — click-to-move demo with funnel-smoothed paths
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `rollback` — `RollbackBuffer`, resimulate — essential for networked browser multiplayer
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)

### 7.2 Visualization & Observability
- [x] (2026-05-06) `debug_draw` — `DrawCommand`, `DrawList` mapped to Float32Array batches for WebGL / WebGPU instancing
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `telemetry` — `PhysicsStats`, rolling averages — bind to browser performance overlay
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `profiler` — `FrameReport`, `to_folded_stacks` — exportable from browser for offline flamegraph analysis
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `scene` — `SceneDescription` JSON round-trip — load authored scenes from `.json` served over HTTP
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)

### 7.3 Scripting-friendly helpers
- [x] (2026-05-06) `interpolator` — `smooth_damp`, `SpringFollower` — critical for camera / UI motion in demos
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `noise` — `ValueNoise3D`, `FractalNoise::turbulence_force` — procedural wind / terrain
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `force_field` — `ForceFieldSystem::apply_to_batch` — explosion / wind / vortex effects
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `snapshot` — `SnapshotManager` — undo/redo in authoring demos
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)
- [x] (2026-05-06) `material_table` — `MaterialTable` presets — user-selectable surface types
  - **Planned:** 2026-05-04 — Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers)

### 7.4 Deferred from web (use Python for these)
Python Phase 6 covers the full surface; these Rust modules are **not** bound in WASM because the browser rarely needs them: `event_bus` (use JS `EventTarget`), `replay` (use browser devtools), `buoyancy` (heavy, niche), `contact_cache` (internal optimization), `scheduler` (internal), `spatial_grid` (internal), `lod` (internal), `xpbd` (heavy, use rigid + rope instead), `aero` (niche).

### 7.5 Packaging & Demos
- [x] Replace `(0.2.0)` placeholders in Phase 5/6 with concrete tasks: `wasm-pack build --target web --release --out-dir pkg/`, `npm publish` pipeline, CI job
  - **Goal:** Four runnable browser demos (character/rope/sph-pool/navmesh) live under `crates/oxiphysics-wasm/web-demos/` and consume the wasm-pack `pkg-web` build. Placeholder fake `pkg-bundler/*` stubs are removed/`.gitignore`d. A landing `index.html` lists all four demos. The CI workflow uploads the demos as an artifact.
  - **Design:**
    1. **Layout**: `web-demos/{index.html, README.md, shared/{canvas-util.mjs,ui-util.mjs}, character/{index.html,main.mjs}, rope/{index.html,main.mjs}, sph-pool/{index.html,main.mjs}, navmesh/{index.html,main.mjs}}`
    2. **wasm-pack build**: each demo imports from `../../pkg-web/oxiphysics.js`; README documents build + serve steps.
    3. **Placeholder cleanup (git rm)**: run `git ls-files crates/oxiphysics-wasm/pkg-bundler/` to enumerate tracked stubs; verify each file size matches 428B/203B/42B via `wc -c`; use `git rm --` (NOT plain rm) for each verified stub; add `pkg-bundler/`, `pkg-web/`, `pkg-node/` to `.gitignore`. Same treatment for `demo/` stubs (verify first).
    4. **Landing `index.html`**: simple grid of 4 demo tiles with SVG placeholder thumbnails.
    5. **CI**: add "Upload demos artifact" step to `.github/workflows/npm-publish.yml`.
    6. **Each demo**: Character (64×64 heightfield, WASD+mouse-look, WebGL2), Rope (32-segment ragdoll, mouse drag), SPH pool (5000 particles, splash impulse), Navmesh (click-to-move path). ESM only, vanilla WebGL2, ≤300 lines JS each.
    7. **JS/TS style**: ESM `.mjs` only, no bundler, no Three.js.
  - **Files:** `web-demos/` (~13 new files); `tests/wasm_bridge_smoke.rs` (new `#[wasm_bindgen_test]` per bridge type); `Cargo.toml` (add `wasm-bindgen-test` dev-dep if absent); `.gitignore` (new); `pkg-bundler/` (git rm stubs); `demo/` (git rm if tracked stubs found); `.github/workflows/npm-publish.yml` (demos artifact); `TODO.md`
  - **Tests:** Programmatic `#[wasm_bindgen_test]` smoke per bridge type in `tests/wasm_bridge_smoke.rs`; build verification (`wasm-pack build --target web`); manual `SMOKETEST.md`; compile guard (`cargo check --target wasm32-unknown-unknown`).
  - **Risk:** `git rm` stubs: `git ls-files` + `wc -c` size-match check guards against removing real build artifacts. JS runtime errors mitigated by `wasm_bindgen_test` smoke + SMOKETEST.md.
- [x] Browser demo: character controller on heightfield (headline)
- [x] Browser demo: rope ragdoll with mouse drag
- [x] Browser demo: SPH pool (GPU when wgpu backend lands, CPU fallback today)
- [x] Browser demo: navmesh click-to-move
